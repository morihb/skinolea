# Leave blank if the frontend and backend are served from the same origin
# (default setup: Express serves the built client). Only set this if you
# deploy the API on a different domain/host than the frontend.
VITE_API_URL=
